import {
    DomainData,
    ReducerGroup,
} from '../../interface';

// ACTION-TYPE


// ACTION-CREATOR

// HELPER

// REDUCER

const reducer: ReducerGroup<DomainData> = {};

export default reducer;
